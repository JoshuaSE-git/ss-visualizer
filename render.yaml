services:
  - type: web
    name: flask-react-app
    env: python
    buildCommand: |
      pip install -r requirements.txt
      cd frontend
      npm install
      npm run build
    startCommand: gunicorn app:app
    plan: free
